apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "streaming-app.fullname" . }}-admin
  labels:
    {{- include "streaming-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: admin
spec:
  replicas: {{ .Values.adminService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "streaming-app.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: admin
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "streaming-app.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: admin
    spec:
      containers:
        - name: admin-service
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.adminService.image }}:{{ .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.adminService.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "streaming-app.fullname" . }}-config
            - secretRef:
                name: {{ include "streaming-app.fullname" . }}-secret
          env:
            - name: PORT
              value: {{ .Values.adminService.port | quote }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.adminService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "streaming-app.fullname" . }}-admin
  labels:
    {{- include "streaming-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: admin
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.adminService.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "streaming-app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: admin
