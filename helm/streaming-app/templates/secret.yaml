apiVersion: v1
kind: Secret
metadata:
  name: {{ include "streaming-app.fullname" . }}-secret
  labels:
    {{- include "streaming-app.labels" . | nindent 4 }}
type: Opaque
stringData:
  JWT_SECRET: {{ .Values.authService.jwtSecret | quote }}
